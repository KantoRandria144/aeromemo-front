const ReunionCard = () => {
    interface ProductCardProps {
        title: string;
        price: number;
        reviews: number;
        rating: number; // de 0 à 5
        colors: string[]; // ex: ["black", "blue", "pink", "teal"]
    }
    return (
        <>

        </>
    )
};

export default ReunionCard;